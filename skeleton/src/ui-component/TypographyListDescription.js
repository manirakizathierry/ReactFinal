import Typography from '@mui/material/Typography';

function TypographyListDescription({ text }) {
    return (
        <Typography variant="body1" gutterBottom>
            {text}
        </Typography>
    );
}

export default TypographyListDescription;
